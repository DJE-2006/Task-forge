# Example environment variables for TaskForge
# Copy to `.env` and update values as needed for your environment

# Database Configuration
DB_HOST=localhost
DB_USER=taskflow
DB_PASS=ChangeMe123!
DB_NAME=task_flow_db

# If using XAMPP with root and no password, set:
# DB_USER=root
# DB_PASS=

# Server Configuration
PORT=3000

# Frontend API URL (Vite environment variable)
VITE_API_URL=http://localhost:3000
